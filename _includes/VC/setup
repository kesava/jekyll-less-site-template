{% capture jbcache %}
  <!--
  - Dynamically set liquid variables for working with URLs/paths
  -->
  {% if site.VC.setup.provider == "custom" %}
    {% include custom/setup %}
  {% else %}
    {% if site.safe and site.VC.BASE_PATH and site.VC.BASE_PATH != '' %}
      {% assign BASE_PATH = site.VC.BASE_PATH %}
      {% assign HOME_PATH = site.VC.BASE_PATH %}
    {% else %}
      {% assign BASE_PATH = nil %}
      {% assign HOME_PATH = "/" %}
    {% endif %}

    {% if site.VC.ASSET_PATH %}
      {% assign ASSET_PATH = site.VC.ASSET_PATH %}
    {% else %}
      {% capture ASSET_PATH %}{{ BASE_PATH }}/assets{% endcapture %}
    {% endif %}  
  {% endif %}
{% endcapture %}{% assign jbcache = nil %}